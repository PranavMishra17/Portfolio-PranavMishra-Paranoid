# Local dev env. Copy to .env.local (gitignored) and fill values.
# For Vercel: set the same vars under Project Settings -> Environment Variables.
# Missing values fall back to safe defaults; nothing breaks.

REACT_APP_VIEW_OFFSET=

REACT_APP_PANTRY_ID=
REACT_APP_PANTRY_BASKET=prompt-patrol-scores
